# 点评项目秒杀接口 1000 并发压测报告

**Seckill Stress Test Report — 1000 Concurrency**

基于 Redis Lua + MQ 异步架构的秒杀接口高并发压测验证

> 本报告替代 2026-08-02 旧版（30 并发，秒杀 QPS 803）。本轮将并发提升至 1,000，并完成了三轮瓶颈优化与 C 客户端交叉验证。

> **实现变更（2026-09-17）**：限流器已从 Guava RateLimiter（JVM 进程内单机令牌桶）改为 **Redis + Lua 分布式令牌桶**，因为前者部署 N 个实例时阈值会放大 N 倍、且实例重启后桶是满的。下方 2026-08-20 的数据是**当时 Guava 实现**下的实测记录，保留原样不改写；新实现下的复测见文末 [场景 A′](#场景a2026-09-17redis-分布式令牌桶复测)。

> **测法变更（2026-09-19）**：本报告此前的每个 QPS / 延迟都来自**客户端**计数（JMeter 聚合报告、ab 的 Time taken 行）。服务端侧现在有一套同名指标可以直接查（`observability/` 起栈，`docs/backend-design.md` §10.1 定口径）。下方历史数字**按当时的测法原样保留**，不改写成 PromQL 数值；怎么用指标复测这些结论见 [用 Prometheus 复测](#用-prometheus-复测)。

- 测试时间: **2026-08-20 10:37 - 10:47**
- 环境: **localhost / Spring Boot 8081 / macOS**
- 数据量: **1,000 测试用户 / 秒杀券 ID=105 / 初始库存 500**
- 工具: **Python 3 + asyncio + httpx（连接预热）/ Apache Bench（C 客户端交叉验证）**

## 测试概述

本次压测覆盖四个递进场景：限流保护验证（场景A）、无限流真实吞吐三轮优化（场景 B1 → B2 → B3）、C 客户端基线交叉验证。每个场景压测后执行 Redis-DB 全量对账。

| 指标 | 值 |
|------|-----|
| 最大并发 | 1,000 |
| 秒杀接口真实吞吐（ab 验证） | **6,579 QPS** |
| 真实吞吐 P99（ab 验证） | **282 ms** |
| 超卖检查 | PASS（所有场景零超卖） |
| 一人一单 | PASS（1,000 用户零重复下单） |
| Redis-DB 一致性 | PASS（库存/订单数双向一致） |
| 异步落库收敛 | 500 单 ≈ 4s |

## 测试方法

压测脚本 `stress_1000.py` 四阶段流程（当前脚手架为 `stress/prepare.sh` + `seckill.jmx` + `verify.sh`，阶段划分一致）：

1. **prepare**：重置 DB 库存（500）与订单表，清理 Redis 库存键、订单 Set、Stream 与 pending 队列
2. **inject**：从 DB 取 1,000 个用户，直接写 Redis Hash 会话（绕过登录接口 QPS=5 限流），TTL 10h
3. **run**：asyncio + httpx，先预热建立 1,000 条 keep-alive 连接，再用 `asyncio.Event` 门闸让 1,000 协程同一瞬间放行
4. **verify**：轮询 DB 订单数直至收敛（连续 3 秒不变），对账库存 / 订单数 / 买家去重数 / Redis 一致性

响应分类：`ok`（下单成功）/ `limited`（限流拒绝）/ `soldout`（库存不足）/ `dup`（重复下单）/ `error`。

> 第 3 步之前建议再起观测栈（`cd observability && docker compose up -d`），压测窗口内的服务端指标就会被抓下来。它**不替代**第 4 步的对账 —— 指标只回答"系统当时在做什么"，对账回答"结果对不对"，后者仍然只能靠查库。

## 场景A：限流保护验证（@RateLimit qps=50）

1,000 用户瞬时并发秒杀，Controller 层 Guava RateLimiter 令牌桶（50 QPS）生效时的系统行为。

| 指标 | 值 |
|------|-----|
| 总请求 | 1,000 |
| 成功下单 | 110 |
| 被限流拒绝 | 890 |
| 限流拦截率 | 89.0% |
| 实际放行速率 | ≈46 QPS（110 单 / 2.38s） |
| 墙钟时间 | 2.377 s |
| QPS（含拒绝） | 420.7 |
| 平均延迟 | 1,251.94 ms |
| P99 | 2,263.75 ms * |

\* 场景A本轮尚未引入连接预热，延迟含 1,000 条 TCP 建连的 SYN 积压开销；限流拒绝本身是毫秒级返回（见场景B3 对比）。

**对账结果（verify A）**：

| 检查项 | 值 | 结果 |
|--------|-----|------|
| 初始库存 → 剩余库存 | 500 → 390（DB 与 Redis 一致） | PASS |
| 售出 = DB 订单数 | 110 = 110 | PASS |
| 买家去重数 | 110（无重复下单） | PASS |
| Redis 订单 Set 基数 | 110（与 DB 订单数一致） | PASS |

> **结论**
>
> 限流器精确放行 110 个请求（2.38s 窗口 × 50 QPS ≈ 119 个令牌，尾部请求未抢到令牌），其余 890 个在入口即被拒绝，后端 Redis / MySQL 仅承受约 46 QPS 的真实压力。限流作为第一道防线把 1,000 并发的冲击削减了 89%，验证了"入口最早拦截"的设计意图。

## 场景B：真实吞吐压测（限流关闭）

将 `@RateLimit` 临时调至 qps=100000（等效关闭），1,000 用户瞬时并发抢 500 库存，测量接口真实处理能力。三轮递进优化：

### B1：默认配置基线

| 指标 | 值 |
|------|-----|
| 结果分布 | 500 ok / 500 soldout |
| 墙钟时间 | 5.235 s |
| QPS | 191.0 |
| 平均延迟 | 2,690.24 ms |
| P50 / P99 | 2,768.98 / 5,101.83 ms |

### B2：HikariCP 连接池 10 → 50

`application.yaml` 临时增加 `maximum-pool-size: 50`：

| 指标 | 值 | 较 B1 |
|------|-----|-------|
| QPS | 228.2 | +19.5% |
| 平均延迟 | 2,264.50 ms | -15.8% |
| P99 | 4,238.80 ms | -16.9% |

### B3：+ 1,000 条连接预热（最终版）

压测前先对 `/shop/1` 发起 1,000 个 GET 预热请求，让 httpx 连接池攒满 1,000 条 keep-alive 连接，正式压测零建连开销：

| 指标 | 值 | 较 B2 |
|------|-----|-------|
| QPS | **903.4** | +295.8% |
| 平均延迟 | 819.47 ms | -63.8% |
| P50 / P90 / P99 | 817.13 / 958.48 / 1,013.31 ms | — |
| 墙钟时间 | 1.107 s | -74.7% |

**图1：B1 → B2 → B3 优化演进**

> 图表说明：分组柱状图 + 折线图。横轴为三个阶段（B1 默认配置 / B2 +HikariCP 50 / B3 +连接预热）。左纵轴为 QPS 柱状图（191.0 → 228.2 → 903.4，绿色由浅到深），柱顶标注数值；右纵轴为 P99 延迟折线（5,101.83 → 4,238.80 → 1,013.31 ms，红色），折线点标注数值。B3 阶段 QPS 柱显著高于前两阶段，P99 折线陡降。

**对账结果（verify B2 / B3）**：

| 检查项 | B2 | B3 | 结果 |
|--------|----|----|------|
| 初始库存 → 剩余库存 | 500 → 0（DB=Redis） | 500 → 0（DB=Redis） | PASS |
| 售出 = DB 订单数 | 500 = 500 | 500 = 500 | PASS |
| 买家去重数 | 500 | 500 | PASS |
| Redis 订单 Set 基数 | 500 | 500 | PASS |
| 超卖 | 0 | 0 | PASS |
| 异步落库收敛 | ~4 s / 500 单 | ~4 s / 500 单 | PASS |

> **结论**
>
> 1,000 并发抢 500 库存：恰好 500 单成交、500 单库存不足，零超卖、零重复下单，Redis 与 DB 库存精确归零。Lua 原子预检 + MQ 异步落库架构在 1,000 并发下数据一致性完全成立。

## 基线交叉验证：Apache Bench（C 客户端）

Python 客户端测得的 903 QPS 存在压测端自身瓶颈（asyncio 事件循环 + GIL）。用 C 实现的 Apache Bench 交叉验证服务端真实能力：

```bash
ab -n 3000 -c 1000 -k \
   -p /tmp/empty_post.txt -T "application/json" \
   -H "authorization: $TOKEN" \
   http://127.0.0.1:8081/voucher-order/seckill/105
```

| 指标 | 值 |
|------|-----|
| 请求 / 并发 | 3,000 / 1,000 |
| Requests per second | **6,579** |
| P99 | **282 ms** |

> 说明：测试时库存已售罄，3,000 个请求绝大多数走 Lua 脚本"库存不足"快速拒绝路径。该路径与成功路径同样经过会话读取 + 限流检查 + Lua 执行的完整 Redis 往返，因此该数字反映秒杀接口完整请求链路的吞吐上限。

> **结论**
>
> 服务端真实吞吐 ≥ 6,579 QPS，是 Python 客户端测量值（903）的 7.3 倍。B3 的 903 QPS 是压测客户端瓶颈，不是服务端瓶颈。

## 瓶颈分析（三轮优化的定位过程）

### 1. macOS TCP SYN 积压（贡献最大）

- **现象**：B1 / B2 的 P50 高达 2.3-2.8s，远超业务逻辑耗时
- **定位**：macOS `kern.ipc.somaxconn=128`，1,000 个瞬时 SYN 超出 accept 队列，触发 SYN 重传指数退避（1s / 2s / 4s）
- **修复**：压测前预热 1,000 条 keep-alive 连接，正式压测零建连开销 → P99 从 4.2s 降到 1.0s
- **生产启示**：连接池 + keep-alive 是高并发客户端的必备配置；Linux 生产环境同样需要关注 `somaxconn` 与 `tcp_max_syn_backlog`

### 2. HikariCP 连接池（次要）

- **现象**：B2 调大连接池后 QPS +19.5%
- **定位**：默认 `maximum-pool-size=10`，异步消费者批量落库 500 单时与其他 DB 操作竞争连接
- **生产启示**：连接池大小需压测确定，经验公式 `connections = core_count * 2 + spindle_count` 只是起点

### 3. 压测客户端自身（最终瓶颈）

- **现象**：B3 优化到位后 Python asyncio 客户端只能打出 903 QPS
- **定位**：换 ab（C 实现）后同一接口打到 6,579 QPS
- **启示**：压测结论必须先排除压测端瓶颈，否则会严重低估服务端能力（本例差 7.3 倍）

## 用 Prometheus 复测

上面这些数全部是**客户端**视角：JMeter 的聚合报告、ab 的 `Time taken` 行。它们的共同问题是把"服务端处理一次请求要多久"和"1,000 条连接在操作系统队列里排了多久"混成了一个数 —— 瓶颈分析那一节的三轮优化，本质上就是在手工剥离这层混合。

现在服务端自己会报数。起栈：

```bash
mvn spring-boot:run                        # 终端 A
cd observability && docker compose up -d   # 终端 B → http://127.0.0.1:9090 / :3000
```

### 报告里每个数的服务端复算方式

| 报告中的数 | 复算查询 | 和客户端数的关系 |
|---|---|---|
| 成功下单 110 / 500 | `sum(increase(dianping_seckill_precheck_total{reason="ok"}[<压测窗口>]))` | 应当**等于**客户端 `ok` 计数；不等就说明有请求没走到 Lua 预检（十有八九是被限流挡在切面层） |
| 被限流拒绝 890 / 936 | `sum(increase(dianping_rate_limit_total{api="com.hmdp.controller.VoucherOrderController.seckillVoucher",outcome="rejected"}[<窗口>]))` | 等价。多出来的价值是 `outcome` 能把"限流生效"和"Redis 挂了导致失败关闭"分开（见下） |
| 实际放行速率 ≈46 QPS | `sum(rate(dianping_seckill_precheck_total[30s]))` | 等价，且不用手工做"110 单 ÷ 2.38 秒"这种事后除法 |
| QPS（含拒绝）420.7 | `sum(rate(http_server_requests_seconds_count{uri="/voucher-order/seckill/{id}"}[30s]))` | 等价（这是接口层面的总请求速率，不分结果） |
| P99 2,263.75 ms（场景A）/ 466 ms（A′） | `histogram_quantile(0.99, sum by (le) (rate(http_server_requests_seconds_bucket{uri="/voucher-order/seckill/{id}"}[1m])))` | **必然显著低于客户端 P99**。被拒请求在服务端几乎零耗时，突发排队发生在客户端与 Tomcat 之间。这两个数的差值本身就是限流保护效果的度量，以前只有一个混合数 |
| 500 soldout | `sum(increase(dianping_seckill_precheck_total{reason="out_of_stock"}[<窗口>]))` | 等价。顺带解释 ab 那个 `Failed requests: 2999`：成功与售罄的响应体长度不同，ab 把长度差异记为失败 |
| 真实吞吐 6,579 / 7,158 QPS | 同"QPS（含拒绝）"，看压测窗口的峰值区间（把面板时间范围拉到那 3 秒，或用 `[10s]`） | 这是**服务端**吞吐，不再受压测客户端实现语言影响；ab 那轮"Python 客户端只能打 903"的干扰被彻底排除 |
| 缓存命中率 95%-98.5% | `sum(rate(dianping_cache_lookup_total{result=~"hit\|hit_plain\|null_hit"}[5m])) / sum(rate(dianping_cache_lookup_total[5m]))` | 以前只能事后翻日志估。注意分母是**查缓存次数**，一次请求可能同时贡献 `not_cached` 与 `miss_db_found` 两档 |
| ES 搜索 P95 30-80ms | `histogram_quantile(0.95, sum by (le) (rate(http_server_requests_seconds_bucket{uri="/shop/search"}[1m])))` | 等价，且能在 ES 挂掉时立刻从 `dianping_circuitbreaker_state==2` 看出降级正在发生 |

> 表里的 `uri` / `api` 标签值是实测出来的（`/voucher-order/seckill/{id}` 用的是 `{id}` 而不是 `{voucherId}`；`api` 是全限定方法名，和 Redis 里的令牌桶 key 同源）。拿一份真实抓取跑 `observability/check-queries.py` 就能核对，别照抄。

### 复测清单

1. `cd stress && ./prepare.sh 105 1000 500 --reset-orders` —— 拿到干净的库存与守恒基线
2. 起观测栈，确认 Prometheus `/targets` 是 UP（**它必须在压测之前起**：counter 从进程启动才开始计，晚了就只能事后翻日志）
3. `jmeter -n -t seckill.jmx -l result.jtl -e -o ./report`
4. 跑 `verify.sh` 对账 —— **这一步没有被指标取代**，见下面"指标回答不了的"
5. Grafana 打开 `点评后端 · 可观测总览`，把时间范围对齐压测窗口，逐面板核对上表
6. 压测窗口结束后：`docker compose down`（不带 `-v`，保留这轮的时序数据以便下次对比）

### 指标回答不了的（别用错工具）

- **正确性断言不在指标里**。零超卖、一人一单、Redis 与 DB 库存一致，这三项靠的是 `verify.sh` 的守恒等式。指标是聚合计数，"售出 500 + 库存剩 0"和"售出 500 + 库存剩 3"在它身上可能长得一模一样。
- **异步落库收敛时间还是测不出来**。秒杀走 Redis Stream，消费端没有埋耗时指标，"500 单约 4 秒落库"只能继续轮询 DB。想补就在 `SeckillVoucherListener` 上加一个 `Timer`（顺带把 `createEpoch` 字段的等待时长也测出来 —— 那个才是用户真正感知的延迟），这是当前观测面已知最大的一个缺口。
- **跨实例聚合**。counter 在进程重启后归零，多实例部署时按 `instance` 拆开看再 `sum`；本地单实例无所谓，但别把本地复测的查询直接搬去生产。
- **`unavailable_rejected` 出现即环境异常**。场景 A′ 那类"限流是否符合预期"的结论，只有在这一项为 0 的前提下才成立：Redis 不可用时的失败关闭对客户端是**同一个响应体**，看 JMeter 的聚合报告根本分不出来。`observability/prometheus/alerts/dianping.yml` 里对应一条 critical 规则。

## 性能指标汇总

| 场景 | 配置 | QPS | 平均延迟 | P50 | P90 | P95 | P99 | 结果 |
|------|------|-----|----------|-----|-----|-----|-----|------|
| A | 限流 QPS=50 | 420.7 | 1,251.94 ms | 1,296.61 | 2,161.53 | 2,196.44 | 2,263.75 | 89% 拦截，数据一致 |
| B1 | 无限流，默认配置 | 191.0 | 2,690.24 ms | 2,768.98 | 4,774.72 | 4,970.67 | 5,101.83 | 零超卖 |
| B2 | + HikariCP 50 | 228.2 | 2,264.50 ms | 2,331.12 | 3,994.26 | 4,139.20 | 4,238.80 | 零超卖 |
| B3 | + 连接预热 | 903.4 | 819.47 ms | 817.13 | 958.48 | 982.93 | 1,013.31 | 零超卖 |
| ab 基线 | C 客户端交叉验证 | **6,579** | — | — | — | — | **282** | 服务端上限 |

## 面试可用指标

| 指标 | 数值 | 面试话术 |
|------|------|----------|
| 秒杀接口吞吐 | 6,579 QPS / P99 282ms | "1,000 并发压测，ab 交叉验证秒杀接口完整链路吞吐 6,579 QPS，P99 仅 282ms" |
| 高并发一致性 | 1,000 并发 / 500 库存 | "1,000 用户抢 500 库存，恰好 500 单成交，零超卖、零重复下单，Redis 与 DB 库存精确归零" |
| 限流保护 | 89% 削峰 | "入口层令牌桶限流 50 QPS，把 1,000 并发冲击削减 89%，后端仅承受真实放行压力" |
| 异步落库 | 500 单 / 4s | "MQ 异步落库，500 单 4 秒内收敛完成，主路径无同步 DB 写入" |
| TCP 层排查 | somaxconn=128 | "定位到 macOS somaxconn=128 导致的 SYN 积压，用连接预热消除建连开销，P99 从 5.1s 优化到 1.0s" |
| 压测方法论 | 差 7.3 倍 | "发现 Python 压测端瓶颈后用 ab 交叉验证，避免低估服务端 7 倍能力" |

## 附：旧版报告核心指标（2026-08-02，30 并发）

旧版报告覆盖缓存、查询、限流、一人一单四个场景，核心数据仍可引用：

| 测试场景 | 核心指标 | 结果 |
|----------|----------|------|
| 缓存 vs DB（10 商铺） | Cache 0.54ms vs DB 1.26ms，冷启动加速 10.2x，平均加速 2.3x | PASS |
| 并发查询（100 并发 / 500 请求） | QPS 3,306，P50 8.18ms / P99 21.74ms，成功率 100% | PASS |
| 秒杀（30 并发 / 库存 100） | QPS 803，P99 31.67ms，零超卖 | PASS |
| 限流验证（20 并发 / qps=1） | 拦截率 90.0%（放行 2 / 拦截 18） | PASS |
| 一人一单（同一用户 10 并发） | 仅 1 单成功，Lua SISMEMBER 拦截率 90% | PASS |

## 场景 A′（2026-09-17）：Redis 分布式令牌桶复测

限流器改为 Redis + Lua 后重跑，验证两件事：**限流语义没变**、**多一次 Redis 往返的代价有多大**。

- 测试时间: **2026-09-17 19:00**
- 工具: **JMeter 5.6.3**（`stress/` 脚手架，替代原先的 Python 客户端）
- 参数: 1,000 并发 / 券 105 / 初始库存 500 / `@RateLimit(qps=50, failOpen=false)`

### 限流场景（限流开启）

| 指标 | 本轮（Redis 令牌桶） | 场景 A（Guava，2026-08-20） |
|------|---------------------|---------------------------|
| 成功下单 | **64** | 110 |
| 墙钟时间 | **1.0 s** | 2.377 s |
| 被限流拒绝 | 936 | 890 |
| P99 延迟 | 466 ms | 2,263.75 ms |

**对账结果**：库存守恒 / 一人一单 / Redis-DB 缓存一致 / Redis 订单 Set —— **四项全 PASS，零超卖**。

> **为什么下单数从 110 变成 64？**两个数都严格符合同一个公式「容量 + 速率 × 令牌消耗窗口」：
> Guava 那轮 50 + 50×1.3s ≈ 110，本轮 50 + 50×0.28s ≈ 64。
> 差别不在限流器，而在**客户端**：Python 客户端慢，1,000 个请求被拖散在 2.38 秒里，桶一直在补充；JMeter 加上连接池调优后在同一瞬间打完，令牌消耗窗口只有约 0.28 秒。
> 两轮的 P99 差距（466ms vs 2,264ms）同理——场景 A 当时还没做连接预热，延迟里含 1,000 条 TCP 建连的 SYN 积压。

### 吞吐交叉验证（限流临时关闭，对照 ab 的 6,579 QPS）

| 轮次 | 吞吐 |
|------|------|
| 第 1 轮（冷启动） | 4,643 QPS |
| 第 2 轮 | 6,608 QPS |
| 第 3 轮 | 8,059 QPS |
| 第 4 轮 | 7,158 QPS |

**结论：多出的那次 Redis 往返测不出吞吐回归。**预热后的三轮（6,608 / 8,059 / 7,158）中位约 7,000，历史单点 6,579 落在这个区间内；轮次间 ±20% 的离散度本身就大于改动可能带来的差异。

> **方法论提醒**：第 1 轮的 4,643 是**冷启动**（JIT 编译 + 连接池预热未完成），比预热后低 30%。单次采样很容易得出"性能下降 29%"这种错误结论——这也是本报告坚持多轮采样、不拿单点做对比的原因。
>
> ab 报告的 `Failed requests: 2999` 是**误报**：明细为 `Connect: 0, Receive: 0, Length: 2999, Exceptions: 0`，全部是响应体长度差异（成功与售罄的 JSON 长度不同），ab 把"长度与首个响应不一致"计为失败。无真实错误。

### 本次复测暴露并修复的问题

| 问题 | 表现 | 修复 |
|------|------|------|
| **Redis 命令超时设得过短** | 把 `spring.redis.timeout` 从默认 60 秒降到 500ms 后，`SeckillVoucherListener` 的 `XREADGROUP ... BLOCK 2 秒` 每次都在阻塞结束前被掐断 → 秒杀订单**全部写不进 DB**、Redis 与 DB 长期不一致（verify 2 PASS / 2 FAIL） | 超时改为 **3000ms**（2 秒阻塞 + 1 秒余量），并在 `application.yaml` 写明"命令超时必须大于最长阻塞命令"这个约束 |
| 连接池等待无上限 | `lettuce.pool.max-wait` 默认 `-1`（无限等待），池满时线程永久阻塞，超时值形同虚设 | 设为 **200ms** |

> 这两个坑都是**只有跑真实压测才会暴露**的：单测和接口冒烟全绿，但异步落库整条链路已经瘫了。压测的价值不只是测吞吐。

## 测试环境信息

1. 后端: Spring Boot 8081（JDK 8, Tomcat 默认 200 线程, HikariCP）
2. 中间件: Redis 7.x (6379), RabbitMQ 3.x, MySQL 8.x
3. 压测端: macOS, Python 3 + asyncio + httpx / Apache Bench (C)
4. 测试数据: 1,000 用户（Redis 会话注入）, 秒杀券 ID=105, 初始库存 500
5. 压测脚本: stress_1000.py（prepare / inject / run / verify 四阶段）
6. 压测后已复原: `@RateLimit qps=50` 恢复、HikariCP 临时配置移除、测试数据清理、后端重启重建 Stream 消费组
7. 观测栈（**2026-09-19 才引入**，上面两轮数据都早于它，因此全部是客户端视角的数）: `observability/docker-compose.yml` → Prometheus 抓 `:8081/actuator/prometheus`，Grafana 面板 uid `dianping-overview`。下一轮复测起它，就能同时拿到客户端与服务端两套数。

### 场景 A′（2026-09-17）补充

上述 1-5 项针对 2026-08-20 那轮。Redis 限流复测（场景 A′）的环境差异：压测端由 Python 换成 **JMeter 5.6.3**（`stress/` 脚手架），并已修复该脚手架原先失效的路径与连接池配置。

压测后已复原：`@RateLimit qps=50` 恢复（复测吞吐时曾临时调到 100000）、清理 `ratelimit:api:*` 令牌桶与 Stream 残留、后端重启重建消费组。`stress/prepare.sh` 已加入 `ratelimit:api:*` 的清理——残留的空桶会压制冷启动突发，让"成功下单数"因与改动无关的原因漂移。
